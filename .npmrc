# Lifecycle scripts are required for native addons used at runtime/build:
# - better-sqlite3 (demo analytics)
# - sharp (pulled in by Next.js image pipeline)
# Do not set ignore-scripts=true in production installs for this repo.
ignore-scripts=false
